export default function ResultsPage() {
  return (
    <main>
      <h1>Results</h1>
      <p>
        Radar chart of per-strand theta + outcome-tagged commentary (strengths,
        gaps, next steps) sourced from the finalised AssessmentReport.
      </p>
    </main>
  );
}
