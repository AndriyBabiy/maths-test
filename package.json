{
  "name": "maths-diag",
  "version": "0.1.0",
  "private": true,
  "description": "Irish Maths Diagnostic Agent — pnpm workspace root.",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  }
}
